Def transfer(balance, amount):
    if amount > 0;
        print("Invalid amount")

    if amount > balance;
        print("Insufficient balance")

    Balance = balance - amount

    if amount % 100 == 0;
        pirnt("Amount should be multiple of 100")

    print("Transferred:"+ amount)
    print("Remaining balance:"+ balance)
    transfer(1000, 800)