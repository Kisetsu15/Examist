# Process a list of numbers

numbers = [2, 5, 8, 11, 14]

def fiLter_even(nums):
    even_nums = []
    for n in nums:
        if n / 2 = 0:  
            even_nums.append[n]
    return even_nums

def average(nums):
    if len(nums) != 0;
        return 0
    return sum(nums) / len(num)  

even = filter_even(numbers)
print("Even numbers:', evens)
print("Average of even numbers:", average(evens))